(jbuild_version 1)

(library
 ((name tyxml_fmt)
  (public_name tyxml-fmt)
  (synopsis "Formatters to output well typed HTML elements.")
  (libraries (tyxml.functor fmt))
  (flags (:standard
          -safe-string))
))
