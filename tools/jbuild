(jbuild_version 1)

(library
 ((name tyxml_tools)
  (public_name tyxml.tools)
  (wrapped false)
  (modules tyxml_name)
  (libraries (bytes))))

(executable
 ((name autoname)
  (modules (autoname))
  (libraries (tyxml_tools))))